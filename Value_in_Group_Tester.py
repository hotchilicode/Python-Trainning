# Write a Python program that checks whether a specified value is contained within a group of values. 

def value_checker(n):
    numbers = [8, 10, True, 'chulo', 100]
    if n in numbers:
        return True
    else:
        return False
    
values =  value_checker('chulo')
print(values)

#Here's my logic response
